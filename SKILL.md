---
name: qiq-backend-tech-review
version: 0.7.1
description: 互联网后台技术方案的多维度系统化评审工作流。覆盖正确性 / 完整功能实现、容错 / 可用性、性能、安全、关键存储与数据结构、关键逻辑详细度、运维、简洁性、API 兼容性、仓库规则与禁用项 9 大维度，输出冲突仲裁、统一严重程度与上线门禁结论。把原始诉求作为 FR 对账的最高优先级源头，与 qiq-backend-tech 技术方案生成 skill 同址配对使用。触发：技术方案评审 / 架构评审 / 设计评审 / design review / RFC 评审 / 上线前评审。
---

# qiq-backend-tech-review — 互联网后台技术方案多维校验

## When to use

启用条件（任一即可）：

- 用户给出后台 / 分布式 / 微服务 / 数据系统 / 高并发系统的技术方案 / 设计文档 / RFC，希望做"评审 / review / 挑刺 / 找漏洞 / 上线前检查"。
- 用户希望对一份方案做**多维度系统化检查**，而非单点提问（如只问性能）。
- 用户使用 `qiq-backend-tech` skill 产出了技术方案，希望进入"评审 → 修订"闭环。本 skill 与之配对：spec skill 输出的 FR / NFR / ASMP 编号、章节结构、关键存储与数据结构清单是本 skill 的对账锚点。

不适用：单点技术问题（如"Redis 为什么慢"）；前端 / 移动端 / 算法模型方案；业务需求 / PRD 评审。

## 执行模式（先与用户对齐再开始）

二选一，用户未明确说明时默认"正式评审"，并把当前模式告诉用户、允许切换：

| 模式 | 触发 | 上下文要求 | 是否输出门禁结论 | 适合场景 |
|---|---|---|---|---|
| **正式评审**（默认） | 默认；用户希望严谨 review | 缺关键 4 项必须追问后再开始（业务重要性 / 是否涉及资金 / 当前 QPS / SLA 目标） | 是 | 上线前评审、正式 design review、RFC 评审 |
| **初步扫描** | 用户明确表达"先粗看 / 快速过一遍 / 不用追问" | 允许临时假设继续，但报告头部必须声明置信度、关键假设、需复核结论 | 否，仅输出风险清单 | 初稿自检、早期讨论、需求方快速对齐 |

## Workflow

### Step 1 — 收集审查上下文（必须）

**禁止跳过此步直接 review**：核心系统和内部工具不能用同一把尺子。

#### 1.0 前置文件预读（强制，先于任何评审动作）

> 目的：评审依据**不只是技术方案文本**，还包括方案上游的"原始诉求"。如果方案漏抽 / 误读 / 静默删减原始诉求里的功能点，单看方案永远发现不了——必须用原始记录回溯对账。

**预读文件清单**（`TECH_DESIGN_DIR` 按 §7.1.0 解析，一次评审只解析一次；同一方案的需求记录、报告、备份与中间产物均归属该目录）：

| 优先级 | 文件路径 | 用途 | 缺失时处理 |
|---|---|---|---|
| P0 | `<TECH_DESIGN_DIR>/requirements.md` | **FR 源头之一**（与方案内"需求章节"并列、且优先级更高）；用于发现"方案对原始诉求的漏抽 / 改写 / 静默删减" | 不存在直接跳过；**不**追问、**不**报错；报告头部"原始需求记录路径"填"无" |
| P0 | 原方案文件本身 | 评审主体 | 必须存在；不存在则评审无法进行 |
| P1 | `<TECH_DESIGN_DIR>/intermediates/*.md` | 上次跑的中间产物，可作为对照基线，避免重复检索 | 不存在直接跳过 |

**硬动作**：

1. 按 §7.1.0 解析 `TECH_DESIGN_DIR`；该结果作为本评审全局变量，Step 7 落盘时直接复用，**不得**重新解析到不同路径。
2. 检测 `${TECH_DESIGN_DIR}/requirements.md`；若存在，**真正调用读文件工具读取其完整内容**，作为 Step 1 / Step 4 校验者 1 的输入。
3. `requirements.md` 与方案文本若有功能点 / 业务约束 / 显式拒绝项层面的不一致，按下文"不一致处置"操作；**不得**默认相信方案、忽略原始记录。

**优先级原则（核心）**：

- **原始诉求 ≥ 方案文本**：当 `requirements.md` 中明确写明的功能点 / 拒绝项与方案不一致时，默认相信原始记录；方案需要给出"为什么改 / 是否经过用户确认"的显式说明，否则视为漏抽 / 静默删减。
- `requirements.md` 中标记为"显式拒绝 / 不做 / 暂不支持"的项，方案中**不应**出现实现；如出现，记一条 C-Major（"超目的实现"）。
- `requirements.md` 中标记为"必须 / 强诉求 / P0"的项，方案中**必须**有对应落点；缺失记一条 C-Critical 候选（最终是否升级 Blocker 由 Step 6 门禁判定）。

**不一致处置**：

- 缺漏（requirements.md 有，方案无）→ 校验者 1（§2.1 功能完整性矩阵）按 ❌ 处理，"原文证据"列引用 `requirements.md` 对应段落。
- 改写（语义偏移）→ 按 🟡 处理，"判断依据"中明确指出偏移点。
- 超目的实现（方案有、requirements.md 无）→ 校验者 1 §2.3 按"无主设计 / 超目的实现"处理。

#### 1.1 五块上下文收集

按 [@references/00-context-template.md](references/00-context-template.md) 收集业务 / 技术 / 现有代码库与既有约定 / 系统规模 / SLA 五块。方案中已写明的直接抽取并向用户复核；`requirements.md` 中已写明但方案未抽取的，**优先采用 requirements.md 的值**并标注来源；两者均缺失项标 "未说明"。

**关键 4 项缺项处理**（业务重要性 / 是否涉及资金 / 当前 QPS / SLA 目标）：

- 正式评审：任一缺失必须追问后再继续；如 `requirements.md` 已明确给出，视为不缺。
- 初步扫描：可临时假设并继续，但必须在报告头部列出假设与置信度。

### Step 1.5 — 外部假设核查（防"基础假设崩塌"）

> 实现阶段推翻方案，往往不是"方案内部不自洽"，而是"方案对外部现实（仓库 / 中间件部署形态 / 既有约定）的假设不成立"。本步用证据驱动的方式提前暴露这类风险。

**触发信号（任一即必须执行；都不具备可跳过，但需在报告中说明跳过原因）**：

- 方案首次引入仓库 / 团队从未使用过的中间件、命令族、能力或机制。
- 方案要替换或扩展一个既有机制（核对"现在怎么做、为什么要换"）。
- 方案关键路径依赖某外部能力提供特定语义（如"可靠投递 / 互斥 / 强一致 / 零丢失 / 全局有序"）。

**必做项（凭证据，不凭记忆）**：

1. **既有约定核查**：在仓库代码 / 注释 / 架构规范中检索方案使用的关键能力 / 命令 / 类型名，判断：是否首次引入（命中数为 0）/ 是否已被标记为 `deprecated` / `do not use` / `unreliable` / 仓库是否已有同类机制。
2. **能力假设对照**：把方案中所有"我假设 X 能做到 Y"的论断显式列出，逐条与该依赖在**本系统实际部署形态**下的真实能力比对（不是官方文档的最大能力，而是本系统配置下能拿到的能力）。
3. **替代 / 兜底盘点**：若首次引入或与既有机制冲突，盘点是否有不依赖新机制的等价方案，以及语义不满足时的补偿路径（对账 / reconcile / 全量重同步）。

核查结果在报告"外部假设核查"小节逐项记录：检查项 / 证据来源（含检索命令）/ 与方案假设是否一致 / 冲突点。

**冲突处置**：

- 与既有约定不一致 → 一致性校验者按其"方案 vs 外部现实一致性"清单输出问题（[@references/01-consistency-completeness.md](references/01-consistency-completeness.md)）。
- 能力假设不成立且无兜底 → HA 校验者按其"跨节点协调通道可靠性"清单输出问题（[@references/02-fault-tolerance-ha.md](references/02-fault-tolerance-ha.md)），Step 6 按 [@references/09-severity-and-gate.md](references/09-severity-and-gate.md) 第三节"外部假设错位"规则强制升级为 Blocker。

### Step 1.6 — 仓库规则与禁用事项预扫描（防"违反明确规定的不允许事项"）

> 目的：回答"**技术方案是否违反了当前仓库里面明确规定的不允许事项 / 弃用的公共方法**"。一次扫描，多个校验者复用。

**做法**：按 [@references/11-repo-rules-and-bans.md](references/11-repo-rules-and-bans.md) 的检索范围与产出格式执行：

1. 检索仓库规则文件（cursor rules / coding standards / 安全规范 / SECURITY.md / 架构文档 / 历史复盘 / linter 配置 / CI 拦截规则 等）。
2. 检索代码注释中的 `deprecated` / `do not use` / `禁用` / `已废弃` / `unsafe` 等显式标记。
3. 把所有命中条目归并为**禁用清单**（`BAN-1`、`BAN-2`、…），含：类别 / 条目 / 来源（文件 + 行号）/ 适用范围 / 严重度 / 是否可豁免。
4. 把禁用清单显式列在报告"仓库规则与禁用项预扫描"小节，作为后续校验者的对账锚点。

**空清单处置**：仓库内**无**任何书面禁用规定 → 必须显式记录"未检索到仓库规则文件 / 禁用清单为空"，**不得静默跳过**；不得用"通用经验"代替"仓库书面规则"。

**注入下游**：禁用清单作为上下文注入校验者 1（§12 仓库规则与禁用事项一致性）、校验者 4（安全相关禁用项）、校验者 5（数据 / 字段类型相关禁用项）、校验者 6（运维 / 日志 / 配置相关禁用项）、校验者 8（deprecated 公共方法）。所有命中项最终在报告"仓库规则违反清单"小节集中呈现。

### Step 2 — 识别方案类型（决定校验重点）

可多选；跨类型取并集。校验者编号详见 Step 4 表。

| 方案类型 | 默认强制启用的校验者 | 强化重点 |
|---|---|---|
| 新系统建设 | 1 + 2 + 3 + 5 + 6 + 7 | 端到端完整性、容量评估、运维准入 |
| 老系统改造 | 1 + 6 + 7 + 8 | 兼容性、灰度、回滚、数据迁移 |
| 数据迁移 | 1 + 5 + 6 + 8 | 双写 / 影子读 / 对账、迁移期一致性、回滚 |
| API / 接口设计 | 1 + 4 + 7 + 8 | 契约、幂等、错误码、版本、鉴权 |
| 高并发活动 | 1 + 2 + 3 + 6 + 7 | 容量、限流降级、热点、预案 |
| 支付 / 交易 / 账务 | 全部启用 + 严格模式 | 资金一致性、对账、合规、审计、零丢失 |
| 消息异步化 | 1 + 2 + 5 + 6 + 8 | 可靠投递、幂等消费、积压、协议演进 |
| 存储选型 | 1 + 3 + 5 + 7 | 存储匹配度、数据建模、扩展上限 |
| 容灾改造 | 2 + 5 + 6 + 7 | RPO / RTO、切换、脑裂、回滚 |
| 权限 / 安全改造 | 1 + 4 + 6 | 越权、审计、合规、灰度 |
| 降本 / 性能优化 | 3 + 6 + 7 | 瓶颈定位、量化收益、回滚兜底 |
| 内部工具 / MVP | 1（轻量）+ 7 | 正确性 + 防过度设计 |

识别结果先告诉用户。

### Step 3 — 选择校验者组合

以 Step 2 矩阵为底，按业务重要性调档：

| 重要性 | 深度 |
|---|---|
| 核心系统 | 全部 8 个，严格模式 |
| 重要系统 | 必选组（1 + 7）+ 3~4 个按需 |
| 一般系统 | 必选组（1 + 7）+ 1~2 个按需 |
| 内部工具 / MVP | 仅 1（轻量）+ 7 |

把"本次启用了哪几个校验者、为什么"先告诉用户，允许增减。范围限定评审（如"只看性能"）下仅展开指定校验者；若发现明显跨维度 Blocker，必须在报告"额外风险提示"小节简述并建议补充评审范围。

### Step 4 — 逐个校验者执行

对选中的每一个校验者：

1. 读取对应 reference 文件（见下表），完全采用其中定义的 Role / Identity / 审查清单 / Rules——这是该校验者的 system prompt，不要改写或缩水。
2. **Output Format 统一使用 [@references/09-severity-and-gate.md](references/09-severity-and-gate.md) 第七节模板**（位置 / 原文证据 / 判断依据 / 修改建议 + 维度专属字段），编号前缀按下表。
3. 把 Step 1 收集的上下文 + Step 1.0 预读的 `requirements.md`（如存在）+ Step 1.5 外部假设核查结论 + Step 1.6 禁用清单注入当前校验者的视角，逐项过审查清单。
4. **强制输出的对账矩阵**（缺一不可，否则该校验者视为未交付）：
   - 校验者 1：**功能完整性矩阵**（FR → 设计落点；FR 来源 = `requirements.md`（如存在，**优先**）∪ 方案内需求章节，每条 FR 标注"FR 来源"列）+ **NFR 满足度矩阵**（NFR → 支撑设计）+ **关键逻辑详细度统计** + **仓库禁用项命中清单**（消费 Step 1.6 产出）。
   - 校验者 2：**可用性 NFR 满足度矩阵**。
   - 校验者 3：**性能 NFR 满足度矩阵**。
   - 校验者 4：**安全 NFR 满足度矩阵**。
   - 校验者 5：**关键存储与关键数据结构齐备度矩阵**。
5. 给出该维度的**总结 + 评分（1–10，按 09 第六节 Rubric）**。

| # | 校验者 | Reference |
|---|---|---|
| 1 | 一致性与完整性（含功能完整性 / NFR 对账 / 关键逻辑详细度 / 仓库禁用项一致性） | [@references/01-consistency-completeness.md](references/01-consistency-completeness.md) |
| 2 | 容错与高可用（含可用性 NFR 对账） | [@references/02-fault-tolerance-ha.md](references/02-fault-tolerance-ha.md) |
| 3 | 性能与可扩展性（含性能 NFR 对账） | [@references/03-performance-scalability.md](references/03-performance-scalability.md) |
| 4 | 安全性（含安全 NFR 对账） | [@references/04-security.md](references/04-security.md) |
| 5 | 数据一致性与存储（含关键存储与数据结构齐备度） | [@references/05-data-storage.md](references/05-data-storage.md) |
| 6 | 可运维性 | [@references/06-operability.md](references/06-operability.md) |
| 7 | 简洁性（奥卡姆剃刀） | [@references/07-simplicity.md](references/07-simplicity.md) |
| 8 | API 契约与兼容性 | [@references/10-api-contract-compatibility.md](references/10-api-contract-compatibility.md) |
| — | 仓库规则与禁用事项（预扫描，不是独立校验者，但产出物需贯穿其它维度） | [@references/11-repo-rules-and-bans.md](references/11-repo-rules-and-bans.md) |

**所有发现强制带证据**：每个问题必须包含位置、原文证据（缺失则写"方案未说明"）、判断依据、修改建议。

### Step 5 — 汇总仲裁

按 [@references/08-orchestration.md](references/08-orchestration.md) 第二节的优先级排序、量化判断与不可妥协红线做最终裁决。一句话原则：**正确性 > 安全 > 数据 > 需求驱动的健壮性 > 兼容性 > 简洁性 > 非需求驱动的健壮性 / 性能**。

### Step 6 — 严重程度归一化与门禁判定

汇总所有问题前，必须执行：

1. **归一化**：按 [@references/09-severity-and-gate.md](references/09-severity-and-gate.md) 第二节映射表，把各校验者原始等级（Critical/P0/High 等）统一映射到 5 档（Blocker/High/Medium/Low/Info）。
2. **Blocker 自动判定**：按 09 第三节规则，把符合条件的问题强制升级为 Blocker。
3. **门禁判定**：按 09 第四节规则机械推导"是否可进入下一阶段"。**不允许凭主观判断给结论**。
4. **豁免登记**：High 及以下问题如不修复，必须按 09 第五节登记 Waiver；Blocker 不可豁免。

> 初步扫描模式跳过本步的"门禁判定"，仅做归一化和风险列举。

### Step 7 — 输出最终报告

#### 7.1 产物落盘规则（强约束）

> **本步是动作指令，不是规则描述**。Agent 必须**真正调用文件写入工具**把报告内容写到磁盘上的目标文件；只在对话里贴出报告内容**不算**完成本步。

**统一产物根目录（强制）**：本 skill 的**所有产物**——最终评审报告、上一版备份、所有中间产物——一律落盘到 **`TECH_DESIGN_DIR`**，不得散落到原方案目录、`.qiqskills/backend-tech/` 根目录或其它位置。与 `qiq-backend-tech` skill 配对，形成"方案产出 ↔ 评审产物"的同址闭环。

#### 7.1.0 `TECH_DESIGN_DIR` 解析规则（**全文唯一权威定义**）

> `TECH_DESIGN_DIR` 是**方案级产物目录**：`<ROOT>/.qiqskills/backend-tech/<PLAN_DIR_NAME>/`。其中 `<ROOT>` 为 `REPO_ROOT`，无仓库根时为 `<CWD>`；读（Step 1.0）和写（Step 7）必须复用同一次解析结果。

**解析步骤**：

1. **定位 `REPO_ROOT`**：从原方案文件所在目录开始逐级向上回溯，遇到第一个含 `.git` / `.hg` / `.svn` 标识的目录即为 `REPO_ROOT`；未命中则视为"无仓库根"并使用 `<CWD>` 兜底。粘贴文本场景以当前工作目录作为起点。
2. **确定方案目录名 `PLAN_DIR_NAME`**：原方案来自文件时，优先用显式方案名称，否则用原方案文件名（不含扩展名）；用户直接粘贴文本时，优先用用户给出的方案名称，否则用 `tech-review-YYYYMMDD-HHmmss`。目录名需安全化：去掉首尾空白，并将 `/`、`\\`、`:`、`*`、`?`、`"`、`<`、`>`、`|`、连续空白替换为 `-`；处理后为空则兜底为 `tech-review-YYYYMMDD-HHmmss`。
3. **计算 `TECH_DESIGN_DIR`**：`(<REPO_ROOT> 或 <CWD>)/.qiqskills/backend-tech/<PLAN_DIR_NAME>`；不存在则在写场景下真实创建。
4. **禁止错误层级**：单个方案的产物不得直接放在 `.qiqskills/backend-tech/` 根目录，也不得放在方案文件所在子目录；如发现历史散落，可在报告"备注"中提示清理。

**目录布局**：

```
<REPO_ROOT>/
├── <方案路径>/<原方案文件>.md                      # 方案可在仓库任意子目录
└── .qiqskills/backend-tech/                        # skill 工作根目录（不是单方案产物根）
    └── <方案目录名>/                               # 方案级产物根目录（= TECH_DESIGN_DIR）
        ├── requirements.md                         # 由 spec skill 产出；本 skill 只读不写
        ├── <原文件名>-review.md                    # 最终评审报告（稳定路径，重跑覆盖写）
        ├── <原文件名>-review.bak.YYYYMMDD-HHmmss.md # 上一版备份（重跑前重命名而来）
        └── intermediates/                          # Step 1 / 1.5 / 1.6 中间产物（建议产出）
```

**硬动作（必做，按顺序执行）**：

1. 按 §7.1.0 解析 `PLAN_DIR_NAME` 与 `TECH_DESIGN_DIR`，并确保目录存在。
2. 计算 `REPORT_PATH`：文件方案使用 `<TECH_DESIGN_DIR>/<原方案文件名>-review.md`；粘贴文本无原文件名时使用 `<TECH_DESIGN_DIR>/<PLAN_DIR_NAME>-review.md`。
3. 若 `REPORT_PATH` 已存在，先重命名为 `<同名>.bak.YYYYMMDD-HHmmss.md` 作为上一版备份（用户明确"不需要备份"时才可跳过）。
4. **调用可写入文件的工具**，把**完整最终报告 Markdown**写入 `REPORT_PATH`。
5. 在对话中回显 `REPO_ROOT` / `PLAN_DIR_NAME` / `TECH_DESIGN_DIR` / `REPORT_PATH` / 上一版备份路径（无则写"无"）。
6. 禁止用对话贴报告替代真实落盘；禁止落盘到 `TECH_DESIGN_DIR` 之外的位置。

**命名规则**：报告与备份位于 `TECH_DESIGN_DIR` 根部；中间产物位于 `<TECH_DESIGN_DIR>/intermediates/`。重跑时报告覆盖稳定路径，中间产物覆盖同名文件，不另作备份。

**中间产物**：Step 1 / 1.5 / 1.6 实际产出后，应同步落盘到 `<TECH_DESIGN_DIR>/intermediates/`，并在最终报告对应小节以**摘要 + 链接**方式引用；如未产出独立文件，最终报告对应小节必须自包含完整内容。

**报告头部必须显式包含的元信息**：原方案路径 / 原仓库根目录（`REPO_ROOT`）/ 方案目录名（`PLAN_DIR_NAME`）/ 原始需求记录路径（不存在填"无"）/ 产物根目录（`TECH_DESIGN_DIR`）/ 报告输出路径（`REPORT_PATH`）/ 上一版备份路径（首次跑填"无"）/ 中间产物清单（未产出填"无"）/ 评审时间（到秒）/ skill 版本号（取自本文件 frontmatter `version` 字段，以 SKILL.md 为唯一权威来源）。

#### 7.2 报告内容结构

按 [@templates/review-report.md](templates/review-report.md) 组织，含 1 个执行摘要 + 11 个正文章节：执行摘要（Top 5 风险 + 门禁结论 + 需业务方确认事项）/ 1 审查上下文 / 2 方案类型与启用的校验者 / 3 仓库规则与禁用项预扫描 / 4 对账矩阵汇总（4.1 功能完整性 + 4.2 NFR 满足度 + 4.3 关键存储与数据结构齐备度 + 4.4 关键逻辑详细度 + 4.5 仓库规则违反清单）/ 5 各校验者发现（5.1–5.8，每个问题保留原始编号 + 归一化严重程度）/ 6 冲突仲裁 / 7 整改清单（按 Blocker/High/Medium/Low/Info 分组）/ 8 豁免登记表（仅 High 及以下，Blocker 不可豁免）/ 9 设计亮点 / 10 整体评分（按 09 §六强制约束计算）/ 11 最终结论（按 09 推导的门禁判定 + 关键风险 + 下一步行动）。

## Pitfalls（高频翻车点反向描述；正向 checklist 见 Verification）

- **跑全部校验者** → 上下文爆炸 + 噪声淹没用户。按方案类型 + 特征裁剪。
- **冲突时不仲裁** → 用户拿到矛盾建议。Step 5 必须给出明确取舍。
- **凭印象写问题 / 不引用方案原文** → 必须引用原文作为证据；方案未说明的明确写出。
- **凭外部依赖的"理论能力"评审** → 出现 Step 1.5 三个信号任一项时必须走 Step 1.5。
- **跳过仓库规则预扫描** → Step 1.6 必须执行；空清单必须显式声明，不得静默跳过。
- **漏读原始需求记录（`requirements.md`）** → Step 1.0 必须先按 §7.1.0 解析 `TECH_DESIGN_DIR`，再检测 `requirements.md` 是否存在；存在就**真读**；不存在显式记录"无"，不得静默跳过。
- **`TECH_DESIGN_DIR` 落地漂移** → 产物只能落到 §7.1.0 解析出的方案级目录，不能落到 `.qiqskills/backend-tech/` 根目录、方案文件所在子目录或其它目录。
- **只列问题不出对账矩阵** → 5 类强制矩阵缺一不可。
- **跳过上下文、输出格式漂移、只列问题不给建议、严重程度跨维度漂移、门禁凭感觉、只挑刺不肯定** → 在 Verification 自检中逐条收敛。
- **Step 7 落盘违规**：①只在对话里贴报告不调用写工具 ②重跑不真备份就覆盖 ③报告头不写 skill 版本号 ④把产物落盘到 `TECH_DESIGN_DIR` 之外 → Step 7 硬动作必须**真的执行**，并在对话里回显 `REPO_ROOT` / `PLAN_DIR_NAME` / `TECH_DESIGN_DIR` / `REPORT_PATH`。

### 反例与正例

**❌ 不合格的发现**：
> 性能可能有问题，建议优化。

**✅ 合格的发现**：
> ### [PERF-3] [瓶颈] [严重程度: High]
> **位置**：方案 §4.2 订单查询接口
> **原文证据**："订单查询通过 SELECT * FROM orders WHERE user_id=? ORDER BY create_time DESC"
> **判断依据**：用户规模 1000 万，单用户历史订单可达数千条；订单表 1 年后预估 5 亿行
> **问题描述**：未限定时间范围 + 无分页 + ORDER BY 非索引前缀，深度查询将导致 P99 飙升至秒级
> **修改建议**：1) 强制时间范围（默认近 90 天）；2) 游标分页；3) (user_id, create_time) 联合索引

**❌ 不合格的"功能完整性"判定**：FR 都基本覆盖了。

**✅ 合格的"功能完整性"判定**：FR-001 / FR-002 / FR-003 ✅；FR-004 🟡（核心路径有，但缺超时/重试设计）；FR-005 ❌（仅在需求章节出现，详设与流程章节均无对应）。Blocker 升级：FR-005 为 P0 级且 ❌ → C-Critical → Blocker。

## Verification

跑完后自检以下条目，全部满足才算合格交付：

- [ ] 已声明执行模式（正式评审 / 初步扫描）。
- [ ] **Step 1.0 前置文件预读已完成**：已按 §7.1.0 解析 `PLAN_DIR_NAME` 与 `TECH_DESIGN_DIR`，并检测 `${TECH_DESIGN_DIR}/requirements.md`；存在则已真读其完整内容并作为 FR 矩阵源头之一（优先级 ≥ 方案内需求章节）；不存在则报告头部显式标注"无"。本次解析的 `TECH_DESIGN_DIR` 与 Step 7 落盘所用是**同一路径**。
- [ ] 已记录关键 4 项核心上下文（业务重要性 / 是否涉及资金 / QPS / SLA；初步扫描模式可为假设）。
- [ ] 若方案首次引入新能力 / 替换既有机制 / 关键路径依赖外部特定语义，已执行 Step 1.5 并在报告中记录证据；否则需在报告中说明跳过原因。
- [ ] 已执行 Step 1.6 并给出禁用清单；空清单已显式声明"未检索到仓库规则文件"。
- [ ] 已识别方案类型，并说明启用了哪几个校验者、为何。
- [ ] **5 类强制对账矩阵齐全**：功能完整性 / NFR 满足度（含可用性 / 性能 / 安全 / 可观测性 4 项强制）/ 关键存储与关键数据结构齐备度 / 关键逻辑详细度 / 仓库规则违反清单。
- [ ] 每个发现都包含：位置、原文证据（或"方案未说明"）、判断依据、统一严重程度、修改建议。
- [ ] 简洁性建议都包含：替代方案、前提条件、失效条件。
- [ ] 严重程度已按 09-severity-and-gate.md 归一化为 Blocker/High/Medium/Low/Info。
- [ ] 已执行 Blocker 自动判定规则（资金 / 数据丢失 / 安全 / 合规 / SLA 错位 / P0 FR 缺失 / 强制 NFR 不满足 / 命中安全相关禁用项 等）。
- [ ] 已给出字段化的整改清单（含 Owner / 验收标准 / 是否阻塞）。
- [ ] 如有豁免，已登记 Waiver（含 owner / 补救方案 / 复盘时间）。
- [ ] 已给出"设计亮点"小节。
- [ ] 正式评审模式下，已按门禁规则给出明确结论；初步扫描模式下，已声明置信度并未输出门禁结论。
- [ ] **已真正调用写文件工具**把报告写入磁盘并给出**绝对路径**；如当前环境无写文件能力，已显式告知用户"未落盘"并标 "未达成"，未做静默降级。
- [ ] **所有产物统一收口到 `TECH_DESIGN_DIR`**：报告、备份、已产出的中间产物一律落于 §7.1.0 解析出的方案级目录；未散落到 `.qiqskills/backend-tech/` 根目录、方案子目录或其它位置；目录已真实创建；对话中已回显 `REPO_ROOT`、`PLAN_DIR_NAME`、`TECH_DESIGN_DIR`、`REPORT_PATH`；重跑场景已真正执行重命名备份动作（不是"声称已备份"）。
- [ ] 报告头部已记录原方案路径、原仓库根目录、方案目录名、原始需求记录路径、产物根目录、报告输出路径、上一版备份路径、中间产物清单、评审时间、skill 版本号；字段值与对话回显路径一致（不是模板占位 `{...}`）。
